<UserControl x:Class="desktop.Views.Apps.Files.VersionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:desktop.Controls"
             AutomationProperties.AutomationId="{Binding DefectMarker}">
    <UserControl.Resources>
        <!--
            The chip renders as an ordinary secondary button in both variants. Its
            internal TextBlock never reaches the tree (the peer suppresses children
            unconditionally), so the caption below is a purely visual affordance and
            the accessible name comes from ActionChip.Label.
        -->
        <Style TargetType="controls:ActionChip">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="controls:ActionChip">
                        <Border x:Name="Chrome" Background="{StaticResource Surface}"
                                BorderBrush="{StaticResource Line}" BorderThickness="1"
                                CornerRadius="6" Padding="14,7">
                            <TextBlock Text="{TemplateBinding Label}" FontSize="13"
                                       Foreground="{StaticResource Ink}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="{StaticResource AccentSoft}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="{StaticResource Canvas}">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel MaxWidth="720" Margin="40,32,40,48">

                <Button Content="← Files" Style="{StaticResource SubtleButton}"
                        HorizontalAlignment="Left" Margin="-8,0,0,16"
                        Command="{Binding GoBackCommand}"/>

                <TextBlock Text="Version history" Style="{StaticResource Title}" Margin="0,0,0,4"/>
                <TextBlock Style="{StaticResource Subtitle}" Margin="0,0,0,20">
                    <Run Text="Saved versions of "/><Run Text="{Binding FileName, Mode=OneWay}"/>
                </TextBlock>

                <Border Style="{StaticResource Card}" Padding="0">
                    <ItemsControl ItemsSource="{Binding Versions}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="{StaticResource Line}" BorderThickness="0,0,0,1" Padding="18,14">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Timestamp}" FontSize="14" FontWeight="Medium"
                                                       Foreground="{StaticResource Ink}"/>
                                            <TextBlock FontSize="12" Foreground="{StaticResource InkMuted}" Margin="0,2,0,0">
                                                <Run Text="{Binding Author, Mode=OneWay}"/><Run Text="  ·  "/><Run Text="{Binding Size, Mode=OneWay}"/>
                                            </TextBlock>
                                        </StackPanel>

                                        <controls:ActionChip Grid.Column="1" VerticalAlignment="Center"
                                                             Label="{Binding RestoreLabel}"
                                                             Command="{Binding DataContext.RestoreCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                             CommandParameter="{Binding}"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Border>

                <TextBlock Text="{Binding StatusMessage}" Margin="2,16,0,0"
                           FontSize="13" Foreground="{StaticResource Positive}"/>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
